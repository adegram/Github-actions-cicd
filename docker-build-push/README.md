# About the project

GitHub Actions builds an example web image on pull requests and publishes tagged images to GHCR from main.

## Run or validate

```sh
Workflow: ../.github/workflows/docker-build-push.yml. Configure GHCR package visibility/permissions as needed.
```